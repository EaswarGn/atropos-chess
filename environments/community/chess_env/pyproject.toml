[project]
name = "chess-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "atroposlib[dev]>=0.4.0",
    "chess>=1.11.2",
]

[tool.uv.sources]
atroposlib = { path = "../../../", editable = true }
